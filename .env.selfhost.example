# Morning Deck - Self-Hosted Configuration
# Copy this file to .env and configure your settings
# Run: docker compose -f docker-compose.selfhost.yml up -d

# ===========================================
# Required: OpenAI / LLM Configuration
# ===========================================
# Your OpenAI API key (get one at https://platform.openai.com/api-keys)
OPENAI_API_KEY=your-api-key-here

# API Base URL - change this to use alternative providers:
# - OpenAI (default): https://api.openai.com
# - OpenRouter: https://openrouter.ai/api/v1
# - Ollama (local): http://host.docker.internal:11434/v1
# - Any OpenAI-compatible API
OPENAI_BASE_URL=https://api.openai.com

# Model names (must be compatible with your API provider)
# For faster/cheaper tasks (summaries, categorization)
OPENAI_MODEL_LITE=gpt-4o-mini

# For complex tasks (briefing generation)
OPENAI_MODEL_HEAVY=gpt-4o

# ===========================================
# Database Configuration
# ===========================================
DB_NAME=morningdeck
DB_USERNAME=postgres
# Change this for production deployments!
DB_PASSWORD=postgres

# ===========================================
# Application URLs
# ===========================================
# Your domain (with port if non-standard)
APP_DOMAIN=localhost:8080

# Full URL to your application
APP_URL=http://localhost:8080

# CORS origins (usually same as APP_URL)
CORS_ALLOWED_ORIGINS=http://localhost:8080

# Application display name
APP_NAME=Morning Deck

# Port to expose (default: 8080)
PORT=8080

# ===========================================
# Optional: Meilisearch (instant search)
# ===========================================
# Enable Meilisearch for instant article search
# Note: Requires running Meilisearch separately
MEILISEARCH_ENABLED=false
MEILISEARCH_HOST=http://meilisearch:7700
MEILISEARCH_API_KEY=

# ===========================================
# Optional: Reddit API
# ===========================================
# Enable Reddit as a feed source
# Get credentials at https://reddit.com/prefs/apps
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=

# ===========================================
# Performance Tuning
# ===========================================
# JVM options for the backend container
JAVA_OPTS=-XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0
